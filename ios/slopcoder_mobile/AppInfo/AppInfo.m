#import <React/RCTBridgeModule.h>

// The Swift class above, exposed to JavaScript — the same shape as
// PushRegistrar, for the same reason: no generated Swift header, no bridging
// apparatus in an otherwise pure-Swift target.
@interface RCT_EXTERN_MODULE (AppInfo, NSObject)

@end
